# Research and reading reliability

The September 2026 Glow parity work transfers the following protections while retaining Sattva's authenticated Family bridge, business map, original sources and current Ask Research product state.

- Original source rows keep at least an 8% reserve beside derived business context. Optional descriptions are shortened before a ready source loses its first row. Counts of included/omitted records and source failures remain explicit; the packet is still bounded. Private holding identities and weights use their existing separate contract.
- Explicit document questions prioritise literal attachment names/text ahead of generic recent company posts. A matching filename does not establish that the document contents were read.
- Answer streams finish with a named success or error, including a failed terminal write followed by a readable teardown. A disconnected reader cannot be guaranteed delivery. Partial text never establishes successful completion.
- Simultaneous forced portfolio callers can share a queued read. Once source access starts, a later forced caller waits for a new read. Each operation owns its state, preventing older queued operations from marking newer ones started. Authentication, origin checks, cancellation acknowledgement, ephemeral storage and failed-read behavior remain unchanged.
- A usable price quote without a 30-session base retains the company's daily breakout grade with its actual date. The table and export distinguish that grade from a capture grade. Missing bases remain unavailable; invalid saved filter identifiers revert visibly to supported defaults.
- Windowed lists measure new row heights before yielding to another task, then restore the held record. Native scrollbar drags still freeze geometry and defer additions/reordering until release. Corrections and removals remain immediate. This only bounds the rendered rows; source history, search and export remain complete.

Checks include the 142-profile/all-source evidence reproduction, literal PDF filename ranking, abnormal stream endings, serialized forced reads, real breakout browser fixtures and a returning service-worker upgrade, plus 50,000-row geometry tests and the full retained All Alerts browser suite. Tests use local fixtures and no paid model, market-provider or private account calls.
