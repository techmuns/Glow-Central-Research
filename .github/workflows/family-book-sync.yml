name: Family book sync
# The same GlowVentures source powers the Family Book and every research scope.
on:
  workflow_dispatch:
  repository_dispatch:
    types: [family-book-updated, glow-book-updated]
jobs:
  sync:
    uses: ./.github/workflows/series-refresh.yml
    secrets: inherit
